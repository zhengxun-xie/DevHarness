# Dependencies
node_modules/

# Build output
lib/
dist/
*.tsbuildinfo

# Package managers: repo tracks npm's lockfile; ignore pnpm's (local runner only)
pnpm-lock.yaml

# OS / editor
.DS_Store
*.log
*.tmp

# DevBuddy runtime (reviews are project assets; keep them tracked)
# .devbuddy/ is intentionally tracked — it holds review records.
